"use client";

// Placeholder — Agent 2 fills this component
export default function PollingRadar(): JSX.Element {
  return (
    <div aria-label="Polling booth radar placeholder">
      <p>Polling booth radar will be rendered here.</p>
    </div>
  );
}
